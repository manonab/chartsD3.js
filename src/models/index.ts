export interface DataItem {
    label: string;
    percentage: number;
}

export interface GlobalItem {
    value: number;
}

export interface BubbleDatas {
    id: number;
    risk_type: string;
    riskLevel: number;
    label: string;
}
